/**
 * A course is like a lesson, but has no id.
 * This is a indiware thing, if you need, you can merge those with the other lessons.
 */
export interface Course {
	subject: string;
	teacher: number;
}
